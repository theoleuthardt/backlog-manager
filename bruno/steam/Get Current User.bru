meta {
  name: Get Current User
  type: http
  seq: 2
}

get {
  url: {{backendUrl}}/api/user/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Shows steam_id and has_steam_api_key (a boolean - the encrypted key
  itself is never returned) for the logged-in user.
}
