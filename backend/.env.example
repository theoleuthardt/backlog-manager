# SQLAlchemy async connection string (note the +asyncpg driver suffix,
# unlike the frontend's plain postgresql:// URL)
POSTGRES_URL=postgresql+asyncpg://postgres:1234@localhost:5432/backlog-manager-db
